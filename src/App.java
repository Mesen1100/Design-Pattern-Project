import java.util.Date;

import ObserverPatternMembers.Furniture;
import ObserverPatternMembers.Person;

public class App {
    public static void main(String[] args) throws Exception {
        /*  // Observer Pattern
        Person person = new Person("John");

        Furniture chair = new Furniture("Chair", new Date(System.currentTimeMillis() + 10000));
        Furniture table = new Furniture("Table", new Date(System.currentTimeMillis() + 20000));

        person.registerObserver(chair);
        person.registerObserver(table);

        while (true) {
            person.notifyObservers();
            Thread.sleep(1000);
        }
        */
    
    }
}
